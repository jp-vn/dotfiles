return {
  -- "folke/tokyonight.nvim",
  -- priority = 1000,
  -- config = function() vim.cmd "colorscheme tokyonight" end,
  -- "catppuccin/nvim",
  -- name = "catppuccin",
  -- priority = 1000,
  -- config = function() vim.cmd "colorscheme catppuccin-mocha" end,
  "rose-pine/neovim",
  name = "rose-pine",
  priority = 1000,
  config = function() vim.cmd "colorscheme rose-pine" end,
  -- "rebelot/kanagawa.nvim",
  -- priority = 1000,
  -- config = function() vim.cmd "colorscheme kanagawa" end,
  -- "dgox16/oldworld.nvim",
  -- lazy = false,
  -- priority = 1000,
  -- opts = {
  --   variant = "cooler",
  -- },
  -- config = function() vim.cmd "colorscheme oldworld" end,
  -- "oxfist/night-owl.nvim",
  -- lazy = false, -- make sure we load this during startup if it is your main colorscheme
  -- priority = 1000, -- make sure to load this before all the other start plugins
  -- config = function()
  --   -- load the colorscheme here
  --   require("night-owl").setup()
  --   vim.cmd.colorscheme "night-owl"
  -- end,
  --  "L-Colombo/atlantic-dark.nvim",
  --  lazy = false,
  --  priority = 1000,
  --  config = function() vim.cmd "colorscheme atlantic-dark" end,
  -- "sainttttt/flesh-and-blood.vim",
  -- lazy = false,
  -- priority = 1000,
  -- config = function() vim.cmd "colorscheme flesh-and-blood" end,
  -- "nyoom-engineering/oxocarbon.nvim",
  -- lazy = false,
  -- priority = 1000,
  -- config = function() vim.cmd "colorscheme oxocarbon" end,
  -- "rjshkhr/shadow.nvim",
  -- priority = 1000,
  -- config = function()
  --   vim.opt.termguicolors = true
  --   vim.cmd.colorscheme "shadow"
  -- end,
}
